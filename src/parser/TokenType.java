package parser;

public enum TokenType {
    DEF,
    ID,
    RETURN,
    NUM,
    REAL,
    BIN_NUMBER,
    EOF,
    OPEN_BRACKET,
    CLOSE_BRACKET,
    COLON,
    INDENT,
    CHARACTER,
    STRING,
    MINUS,
    DIVISION,
    EQ,
    OR,
    MUL,
    IF,
    ELSE,

}
